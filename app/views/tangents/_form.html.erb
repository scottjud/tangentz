<%= form_for(@tangent) do |f| %>
  <% if @tangent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tangent.errors.count, "error") %> prohibited this tangent from being saved:</h2>

      <ul>
      <% @tangent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
